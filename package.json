{
    "name": "aisitebuilder-monorepo",
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "build": "npm run build --prefix frontend",
        "start": "npm start --prefix frontend",
        "install-all": "npm install --prefix frontend && npm install --prefix backend"
    }
}